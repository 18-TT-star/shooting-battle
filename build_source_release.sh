#!/bin/bash

echo "=============================================="
echo "  Bob's Big Adventure - ソース版パッケージ作成"
echo "=============================================="
echo

# リリースディレクトリを準備
SOURCE_DIR="release/BobsBigAdventure_Source"
rm -rf "$SOURCE_DIR"
mkdir -p "$SOURCE_DIR"

echo "📦 ファイルをコピー中..."

# ゲームファイルをコピー
cp shooting_game.py "$SOURCE_DIR/"
cp player.py "$SOURCE_DIR/"
cp bullet.py "$SOURCE_DIR/"
cp gameplay.py "$SOURCE_DIR/"
cp ui.py "$SOURCE_DIR/"
cp fonts.py "$SOURCE_DIR/"
cp constants.py "$SOURCE_DIR/"
cp save_system.py "$SOURCE_DIR/"
cp setup_and_play.py "$SOURCE_DIR/"

# assetsフォルダをコピー
cp -r assets "$SOURCE_DIR/"

# savesフォルダを作成
mkdir -p "$SOURCE_DIR/saves"

# requirements.txtを作成
cat > "$SOURCE_DIR/requirements.txt" << 'EOF'
pygame>=2.0.0
numpy
pyttsx3>=2.90
EOF

# ユーザー向けREADMEを作成
cat > "$SOURCE_DIR/README.txt" << 'EOF'
==================================================
     Bob's Big Adventure - クロスプラットフォーム版
==================================================

このバージョンは Windows / Linux / Mac 全てで動作します！


【必要なもの】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Python 3.8 以上

まだインストールしていない場合:
→ https://www.python.org/downloads/
  からダウンロードしてインストール


【遊び方（超簡単！）】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

■ Windows の場合:
  1. setup_and_play.py をダブルクリック
     または
     コマンドプロンプトで: python setup_and_play.py

■ Linux / Mac の場合:
  ターミナルで: python3 setup_and_play.py

初回起動時に必要なライブラリが自動インストールされます！
2回目以降は即座にゲームが起動します。


【操作方法】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▼ タイトル画面
  スペースキー - ゲーム開始 / 進行
  T キー - タイトル画面に戻る
  ESC キー - ゲーム終了

▼ ステージ選択
  ↑↓ キー - ステージ選択
  スペースキー - 決定
  ESC / T キー - タイトルに戻る

▼ バトル画面
  矢印キー - プレイヤー移動
  スペースキー - 弾を発射
  P キー - ポーズ
  T キー - タイトルに戻る（ポーズ中）

▼ 装備画面
  ↑↓ キー - アイテム選択
  スペースキー - 装備ON/OFF
  ESC / T キー - タイトルに戻る


【ゲーム目標】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✦ 全6ステージのボスを倒してゲームクリア！
✦ ステージクリア後、便利な装備アイテムを獲得
✦ 全ステージクリア後、装備なしモードに挑戦可能
✦ 装備なしモード全クリアで真のエンディング！


【セーブデータ】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
セーブデータは saves フォルダに保存されます。
3つのセーブスロットが使用可能です。


【トラブルシューティング】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Q: "Python が見つかりません" と表示される
A: Python をインストールしてください
   → https://www.python.org/downloads/
   インストール時に "Add Python to PATH" に必ずチェック！

Q: pip が動かない
A: コマンドで手動インストール:
   Windows: python -m pip install pygame numpy
   Linux/Mac: python3 -m pip install pygame numpy

Q: 画面が真っ暗 / 音が出ない
A: - グラフィックドライバを最新版に更新
   - システムの音量設定を確認
   - assetsフォルダが同じ場所にあるか確認

Q: "ModuleNotFoundError" エラー
A: setup_and_play.py を使って起動してください
   直接 shooting_game.py を実行すると依存関係が解決されません


【ファイル構成】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BobsBigAdventure_Source/
├── setup_and_play.py      ← これを実行！
├── shooting_game.py        (メインプログラム)
├── player.py
├── bullet.py
├── gameplay.py
├── ui.py
├── fonts.py
├── constants.py
├── save_system.py
├── requirements.txt
├── assets/                 (音楽・フォント)
│   └── audio/
└── saves/                  (セーブデータ)


【動作環境】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Python 3.8 以上
- 画面解像度: 800x600 以上推奨
- 対応OS: Windows 7以降 / Linux / macOS 10.13以降


【クレジット】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Created with Python & Pygame

楽しんでください！ (๑˃̵ᴗ˂̵)و

==================================================
EOF

# Windows用バッチファイルを作成
cat > "$SOURCE_DIR/PLAY.bat" << 'EOF'
@echo off
title Bob's Big Adventure
python setup_and_play.py
pause
EOF

# Linux/Mac用シェルスクリプトを作成
cat > "$SOURCE_DIR/PLAY.sh" << 'EOF'
#!/bin/bash
cd "$(dirname "$0")"
python3 setup_and_play.py
EOF

chmod +x "$SOURCE_DIR/PLAY.sh"

echo "📝 README.txt を作成しました"
echo "🎮 ランチャースクリプトを作成しました"

# ZIPアーカイブを作成
echo "📦 ZIPアーカイブを作成中..."
cd release
zip -r -q Bobs_Big_Adventure_v1.0_Source.zip BobsBigAdventure_Source/
cd ..

echo
echo "=============================================="
echo "✨ ソース版パッケージ作成完了！"
echo "=============================================="
echo
echo "📦 配布ファイル:"
echo "   release/Bobs_Big_Adventure_v1.0_Source.zip"
echo
echo "📂 展開済みフォルダ:"
echo "   release/BobsBigAdventure_Source/"
echo
echo "このZIPファイルを配布すれば、"
echo "Windows / Linux / Mac 全てで動作します！"
echo
